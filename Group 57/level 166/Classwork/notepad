XMLHttpRequest() ქმნის“კავშირს ვებსაიტსა და სერვერს შორის, რომ მონაცემები წამოიღოს ან გაგზავნოს.
ეს არის ინსტრუმენტი, რომლითაც JavaScript ინტერნეტიდან იღებს ინფორმაციას (API)

onreadystatechange ეს არის ფუნქცია, რომელიც ირთვება მაშინ, როცა request-ის მდგომარეობა იცვლება.
“როცა რაღაც ახალი ხდება request-ში — ეს კოდი მუშაობს”

this.readyState ეს აჩვენებს request-ის ეტაპს.

მნიშვნელობები:

0 = არ დაწყებულა
1 = დაიწყო
2 = მიიღო პასუხი
3 = იტვირთება
4 = დასრულდა ✅

this ნიშნავს “ამჟამინდელ ობიექტს”, სადაც კოდი მუშაობს.

this = XMLHttpRequest ობიექტი (http)

JSON.parse()

იღებს ტექსტს (string)
და აქცევს JavaScript object/array-ად

'{"name":"Luka"}'  →  {name: "Luka"}

JSON.stringify()

JavaScript object-ს ან array-ს აქცევს ტექსტად (string-ად).
კომპიუტერები ხშირად მონაცემებს აგზავნიან როგორც JSON ტექსტს
ინტერნეტში (API-ზე) მონაცემები ჩვეულებრივ გადადის როგორც string

localStorage არის ბრაუზერში არსებული პატარა საცავი, სადაც შეგიძლია მონაცემების შენახვა.
ეს არის “მინი მეხსიერება” შენს საიტისთვის, რომელიც ინახავს მონაცემებს მომხმარებლის კომპიუტერში.
მონაცემები ინახება browser-ში (Chrome, Edge, etc.)
არ იშლება გვერდის refresh-ზე
რჩება მანამდე, სანამ თვითონ არ წაშლი
ინახავს მხოლოდ string (ტექსტს)

login მონაცემების შენახვა (არასენსიტიური!)
theme (dark/light mode)
კალათის მონაცემები (shopping cart)
პატარა აპების state

document.createElement()
ქმნის ახალ HTML ელემენტს JavaScript-ში (მაგრამ ჯერ არ ჩანს გვერდზე)

innerHTML ელემენტის შიგნით წერს ტექსტს ან HTML-ს

appendChild() ამატებს ელემენტს რეალურ გვერდზე (DOM-ში)